# Minecraft 服务器关闭问题说明

## 问题描述

服务器在停止时会卡在以下信息：
```
[MoonriseCommon] Awaiting termination of I/O pool for up to 60s...
```

随后终端或日志中会无限输出 `>>>>>>>>>>>>...` 符号，不会自动结束。

## 问题原因

这是 **Paper 服务器的已知 Bug**，与 Moonrise 引擎的 I/O 线程池关闭机制有关：

1. **I/O 线程池无法正常关闭**：Paper 1.21.x 使用的 Moonrise 引擎在某些情况下，I/O 线程池会卡住无法正常终止
2. **日志输出问题**：无限的 `>` 符号是线程等待超时时的异常输出
3. **nohup 重定向**：使用 `nohup` 和日志重定向可能会加剧这个问题

## 解决方案

### 方案 1：使用改进的停止脚本（推荐）

已更新 `stop.sh` 脚本，会等待 70 秒后自动强制终止：

```bash
bash /root/minecraft-server/stop.sh
```

**特点：**
- 先发送 SIGTERM 信号（正常关闭）
- 等待 70 秒（超过 Paper 的 60 秒超时）
- 如果仍未停止，自动执行 `kill -9` 强制终止

### 方案 2：使用优雅停止脚本

使用新创建的 `stop-graceful.sh`，通过 RCON 发送 stop 命令：

```bash
bash /root/minecraft-server/stop-graceful.sh
```

**特点：**
- 通过 RCON 发送 `stop` 命令（最优雅）
- 等待 75 秒
- 自动强制终止

### 方案 3：手动强制停止

如果脚本无法停止，手动执行：

```bash
# 查找进程 ID
ps aux | grep paper

# 强制终止（替换 <PID> 为实际进程号）
kill -9 <PID>
```

## 预防措施

### 1. 升级 Paper 版本

这个问题在较新版本的 Paper 中可能已修复：

```bash
cd /root/minecraft-server
# 下载最新版本
wget https://api.papermc.io/v2/projects/paper/versions/1.21.8/builds/latest/downloads/paper-1.21.8-latest.jar
# 备份旧版本
mv paper-1.21.8-60.jar paper-1.21.8-60-backup.jar
# 使用新版本
mv paper-1.21.8-latest.jar paper-1.21.8-60.jar
```

### 2. 调整 JVM 参数

在 `start.sh` 中添加以下参数可能有帮助：

```bash
java -Xms2G -Xmx4G \
  -XX:+UseG1GC \
  -XX:+ParallelRefProcEnabled \
  -XX:MaxGCPauseMillis=200 \
  -XX:+UnlockExperimentalVMOptions \
  -XX:+DisableExplicitGC \
  -jar paper-1.21.8-60.jar nogui
```

### 3. 使用 screen 或 tmux

使用 screen/tmux 可以更好地管理服务器进程：

```bash
# 安装 screen
apt-get install screen

# 启动服务器
screen -dmS minecraft bash -c 'cd /root/minecraft-server && java -Xms2G -Xmx4G -jar paper-1.21.8-60.jar nogui'

# 连接到服务器控制台
screen -r minecraft

# 在控制台中输入 stop 命令停止服务器
# 按 Ctrl+A 然后 D 断开连接（不停止服务器）
```

### 4. 检查插件兼容性

某些插件可能导致关闭卡住，尝试：

1. 逐个禁用插件测试
2. 更新所有插件到最新版本
3. 检查以下可疑插件：
   - CoreProtect（数据库操作）
   - AuthMe（数据库操作）
   - LuckPerms（数据库操作）
   - FastAsyncWorldEdit（异步操作）

## 当前状态

- ✅ 已更新 `stop.sh` - 自动等待并强制终止
- ✅ 已创建 `stop-graceful.sh` - 通过 RCON 优雅停止
- ✅ 已更新 `start.sh` - 添加运行检查和更多信息
- ⚠️ 问题根源在 Paper 服务器，等待官方修复

## 快速参考

```bash
# 启动服务器
bash /root/minecraft-server/start.sh

# 停止服务器（自动处理卡住问题）
bash /root/minecraft-server/stop.sh

# 优雅停止（推荐）
bash /root/minecraft-server/stop-graceful.sh

# 查看日志
tail -f /root/minecraft-server/server.log

# 检查服务器状态
ps aux | grep paper
```

## 相关链接

- [Paper GitHub Issues - I/O Pool Shutdown](https://github.com/PaperMC/Paper/issues)
- [Paper Documentation](https://docs.papermc.io/)
- [Moonrise Engine](https://github.com/Tuinity/Moonrise)
