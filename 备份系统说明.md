# 🔄 Minecraft 服务器自动备份系统

## 📋 系统概述

完整的自动备份解决方案，提供**每天6次自动备份**，结合 **CoreProtect 实时回滚**，为您的服务器提供双重保护。

---

## ⏰ 自动备份时间表

### 📅 每天6次备份（集中在玩家活跃时段）

| 时间 | 说明 | 备份类型 |
|------|------|----------|
| **14:00** | 下午开始 | 小时备份 |
| **16:00** | 下午高峰 | 小时备份 |
| **18:00** | 晚饭时间 | 小时备份 |
| **20:00** | 晚间高峰 | 小时备份 |
| **22:00** | 深夜前 | 小时备份 |
| **00:00** | 午夜 | 小时备份 + 每日备份 |

### 📊 备份保留策略

- **小时备份**：保留最近 **24个**（1天）
- **每日备份**：保留最近 **7个**（1周）
- **每周备份**：保留最近 **4个**（1个月）
- **每月备份**：保留最近 **3个**（3个月）

---

## 🛠️ 可用工具

### 1. 自动备份脚本 `auto-backup.sh`

**功能**：
- ✅ 智能热备份（服务器运行时自动禁用保存）
- ✅ 备份世界数据、插件配置、玩家数据
- ✅ 自动压缩（tar.gz 格式）
- ✅ 自动分类存储（小时/每日/每周/每月）
- ✅ 自动清理旧备份
- ✅ 详细日志记录

**手动执行**：
```bash
cd /root/minecraft-server
./auto-backup.sh
```

### 2. 备份恢复脚本 `restore-backup.sh`

**功能**：
- ✅ 交互式选择备份文件
- ✅ 显示所有可用备份
- ✅ 自动创建安全备份
- ✅ 完整恢复世界和配置

**使用方法**：
```bash
cd /root/minecraft-server
./restore-backup.sh
```

**注意**：恢复前必须停止服务器！

### 3. 备份管理工具 `backup-manager.sh`

**功能**：
- ✅ 查看所有备份列表
- ✅ 查看备份统计信息
- ✅ 立即执行备份
- ✅ 删除指定备份
- ✅ 清理所有旧备份
- ✅ 查看备份日志

**使用方法**：
```bash
cd /root/minecraft-server
./backup-manager.sh
```

---

## 📁 备份内容

### ✅ 包含的内容

1. **世界数据**
   - `world/` - 主世界
   - `world_nether/` - 下界
   - `world_the_end/` - 末地

2. **插件配置**
   - 所有插件的配置文件夹
   - 插件数据文件

3. **玩家数据**
   - AuthMe 玩家登录数据
   - LuckPerms 权限数据
   - Essentials 玩家数据

4. **服务器配置**
   - server.properties
   - bukkit.yml
   - spigot.yml
   - paper-global.yml
   - paper-world-defaults.yml

### ❌ 排除的内容

- 日志文件（*.log）
- 缓存文件（cache/）
- 临时文件（*.tmp）
- 服务器核心文件（paper-*.jar）

---

## 🔍 备份目录结构

```
/root/minecraft-server/backups/
├── hourly/          # 小时备份（保留24个）
│   ├── backup-20251005-140000.tar.gz
│   ├── backup-20251005-160000.tar.gz
│   └── ...
├── daily/           # 每日备份（保留7个）
│   ├── backup-20251005-020000.tar.gz
│   └── ...
├── weekly/          # 每周备份（保留4个）
│   └── ...
└── monthly/         # 每月备份（保留3个）
    └── ...
```

---

## 🛡️ 双重保护机制

### 1. 自动备份系统（本系统）

**优势**：
- ✅ 完整的世界数据备份
- ✅ 可恢复到任意时间点
- ✅ 离线存储，安全可靠
- ✅ 适合大规模回滚

**适用场景**：
- 服务器崩溃恢复
- 重大错误回滚
- 数据迁移
- 灾难恢复

### 2. CoreProtect 实时回滚

**优势**：
- ✅ 实时记录所有操作
- ✅ 精确到方块级别
- ✅ 可回滚单个玩家
- ✅ 不影响其他区域

**适用场景**：
- 玩家破坏回滚
- 熊孩子捣乱
- 误操作修复
- 小范围恢复

---

## 📊 存储空间估算

### 当前服务器数据大小
- 世界数据：约 20MB
- 插件配置：约 5MB
- 单次备份：约 8MB（压缩后）

### 存储空间需求
- 小时备份（24个）：约 192MB
- 每日备份（7个）：约 56MB
- 每周备份（4个）：约 32MB
- 每月备份（3个）：约 24MB
- **总计**：约 **304MB**

### 可用空间
- 当前可用：**68GB**
- 备份占用：**0.3GB**
- **剩余空间充足** ✅

---

## 🚀 快速开始

### 1. 测试备份系统

```bash
# 手动执行一次备份
cd /root/minecraft-server
./auto-backup.sh
```

### 2. 查看备份结果

```bash
# 使用管理工具查看
./backup-manager.sh
# 选择 "1. 查看所有备份"
```

### 3. 测试恢复功能

```bash
# 停止服务器
./stop.sh

# 运行恢复脚本
./restore-backup.sh

# 选择要恢复的备份文件
```

---

## ⚙️ 定时任务配置

### 查看当前定时任务

```bash
crontab -l
```

### 修改备份时间

```bash
# 编辑 crontab
crontab -e

# 修改备份时间（格式：分 时 日 月 周）
# 例如：每4小时备份一次
0 */4 * * * /root/minecraft-server/auto-backup.sh >> /var/log/minecraft-backup.log 2>&1
```

### 禁用自动备份

```bash
# 删除所有备份任务
crontab -l | grep -v "auto-backup.sh" | crontab -
```

### 重新启用自动备份

```bash
# 运行配置脚本（如果有）
# 或手动添加到 crontab
crontab -e
```

---

## 📝 查看备份日志

### 实时查看日志

```bash
tail -f /var/log/minecraft-backup.log
```

### 查看最近的日志

```bash
tail -50 /var/log/minecraft-backup.log
```

### 搜索错误日志

```bash
grep "ERROR" /var/log/minecraft-backup.log
```

---

## 🔧 故障排除

### 问题 1：备份失败 - 磁盘空间不足

**症状**：
```
[ERROR] 备份失败：磁盘空间不足
```

**解决方案**：
```bash
# 1. 查看磁盘空间
df -h

# 2. 清理旧备份
./backup-manager.sh
# 选择 "5. 清理所有旧备份"

# 3. 减少保留数量（编辑 auto-backup.sh）
# 修改保留策略变量
```

### 问题 2：备份时服务器卡顿

**症状**：
玩家反馈备份时游戏卡顿

**解决方案**：
```bash
# 1. 调整备份时间到低峰期
crontab -e
# 改为凌晨时段：2:00, 4:00, 6:00 等

# 2. 降低压缩级别（编辑 auto-backup.sh）
# 将 COMPRESSION_LEVEL=6 改为 COMPRESSION_LEVEL=3
```

### 问题 3：恢复后数据丢失

**症状**：
恢复备份后部分数据不见了

**可能原因**：
- 备份时间点在数据创建之前
- 备份不完整

**解决方案**：
```bash
# 1. 查看安全备份
ls -lh /root/minecraft-server/backups/safety-backup-*.tar.gz

# 2. 使用更近的备份
./restore-backup.sh
# 选择更近的时间点

# 3. 使用 CoreProtect 恢复单个区域
# 游戏内执行：/co rollback u:玩家名 t:时间 r:半径
```

### 问题 4：定时任务未执行

**症状**：
到了备份时间但没有执行

**解决方案**：
```bash
# 1. 检查 cron 服务状态
systemctl status cron

# 2. 启动 cron 服务
systemctl start cron
systemctl enable cron

# 3. 检查 crontab 配置
crontab -l

# 4. 查看系统日志
grep CRON /var/log/syslog
```

---

## 🎯 最佳实践

### 1. 定期测试恢复

**建议**：每月测试一次备份恢复流程

```bash
# 在测试环境恢复备份
# 验证数据完整性
```

### 2. 重要操作前手动备份

**重要操作包括**：
- 安装新插件
- 更新服务器版本
- 修改重要配置
- 大规模建筑活动

```bash
# 手动备份
./auto-backup.sh
```

### 3. 监控备份日志

**建议**：定期检查备份日志

```bash
# 每周查看一次
tail -100 /var/log/minecraft-backup.log
```

### 4. 异地备份

**建议**：定期将备份文件复制到其他位置

```bash
# 复制到其他服务器
scp /root/minecraft-server/backups/daily/* user@backup-server:/backups/

# 或上传到云存储
# 使用 rclone、rsync 等工具
```

---

## 📞 CoreProtect 使用指南

### 基本命令

```bash
# 查看方块历史
/co inspect
# 然后左键点击方块

# 回滚玩家操作
/co rollback u:玩家名 t:1h r:10
# u: 玩家名
# t: 时间（1h=1小时, 1d=1天）
# r: 半径

# 恢复回滚
/co restore u:玩家名 t:1h r:10

# 查看区域历史
/co lookup r:10 t:1d

# 清除数据（谨慎使用）
/co purge t:30d
```

### 常用场景

**场景 1：玩家破坏建筑**
```bash
/co rollback u:熊孩子 t:1d r:50
```

**场景 2：TNT爆炸**
```bash
/co rollback t:10m r:20 a:tnt
```

**场景 3：查看谁动了箱子**
```bash
/co inspect
# 右键点击箱子
```

---

## 📈 系统监控

### 备份统计

```bash
# 使用管理工具查看
./backup-manager.sh
# 选择 "2. 查看备份统计"
```

### 磁盘使用

```bash
# 查看备份目录大小
du -sh /root/minecraft-server/backups/*

# 查看磁盘空间
df -h
```

### 定时任务状态

```bash
# 查看最近的 cron 执行
grep CRON /var/log/syslog | tail -20
```

---

## 🎮 总结

您的服务器现在拥有：

✅ **自动备份系统**
- 每天6次自动备份
- 智能分类存储
- 自动清理旧备份

✅ **CoreProtect 实时保护**
- 记录所有玩家操作
- 精确回滚到方块级别
- 支持区域查询

✅ **完善的管理工具**
- 一键备份/恢复
- 图形化管理界面
- 详细日志记录

✅ **充足的存储空间**
- 68GB 可用空间
- 备份仅占 0.3GB
- 可支持长期存储

---

**🎯 现在您的服务器已经拥有企业级的备份保护！**

有任何问题请查看日志或使用管理工具。祝您的 Minecraft 服务器运行愉快！ 🎮
