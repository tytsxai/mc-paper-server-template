# Minecraft 服务器出生点设置说明

## EssentialsSpawn 配置说明

### 当前配置状态
- ✅ EssentialsXSpawn 插件已安装
- ✅ 新玩家欢迎消息已启用
- ✅ 新玩家出生点设置为 "newbies" 组
- ✅ 新玩家会获得初始工具包 (tools)

### 如何设置出生点

#### 1. 设置默认出生点
在游戏中，站在你想设置为出生点的位置，执行：
```
/setspawn
```

#### 2. 设置新玩家专用出生点
为首次加入的玩家设置特殊出生点：
```
/setspawn newbies
```

#### 3. 查看当前出生点
```
/spawn
```

#### 4. 传送到出生点
```
/spawn
```

### 配置选项说明

在 `plugins/Essentials/config.yml` 中的关键配置：

```yaml
newbies:
  # 新玩家首次加入时的欢迎消息
  announce-format: '&dWelcome {DISPLAYNAME}&d to the server!'
  
  # 新玩家出生点组名称
  spawnpoint: newbies
  
  # 新玩家获得的初始工具包
  kit: tools

# 玩家死亡后是否在家重生（而不是出生点）
respawn-at-home: false

# 玩家死亡后是否在床重生
respawn-at-home-bed: true

# 玩家加入服务器时是否传送到出生点
spawn-on-join: false
```

### 推荐设置流程

1. **启动服务器并加入游戏**
2. **找到合适的出生点位置**（建议在平坦、安全的区域）
3. **站在该位置执行命令**：
   ```
   /setspawn newbies
   /setspawn
   ```
4. **测试出生点**：
   ```
   /spawn
   ```

### 其他有用的命令

- `/spawner` - 查看或修改刷怪笼类型
- `/setspawn <group>` - 为特定组设置出生点
- `/delhome <name>` - 删除家的传送点
- `/sethome <name>` - 设置家的传送点

### 注意事项

1. 出生点坐标会保存在 `plugins/AuthMe/spawn.yml` 中
2. 修改配置后需要重启服务器或执行 `/essentials reload`
3. 确保出生点周围安全，没有危险的生物或环境
4. 建议在出生点附近放置告示牌，说明服务器规则

## AuthMe 配置

- ✅ 登录超时时间：**120 秒（2分钟）**
- ✅ 新玩家必须注册才能游戏
- ✅ 未登录玩家无法移动

## 服务器种子

当前使用种子：`-1232144656584` (有趣的地形和结构)
