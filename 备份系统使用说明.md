# Minecraft 服务器备份系统使用说明

## 📦 备份方案概述

本服务器采用**双重备份策略**：
1. **本地备份**: 压缩备份文件存储在 `/root/minecraft-backups/` 目录
2. **GitHub 云端备份**: 将服务器文件同步到 GitHub 仓库

## 🔄 自动备份功能

### 定时任务
系统已配置自动定时备份任务：
- **每天凌晨 3:00** 自动执行备份和推送
- 可选配置：每 6 小时或 12 小时备份一次

### 备份内容
- 服务器配置文件 (server.properties, bukkit.yml 等)
- 所有插件及其配置
- 世界数据（world, world_nether, world_the_end）
- 玩家数据
- 白名单、OP 列表等

### 自动排除的文件
- 日志文件 (*.log, logs/*)
- 缓存文件 (cache/*)
- Git 仓库文件 (.git/*)
- 旧版本 jar 文件 (*.jar.old)

## 📝 使用方法

### 1. 手动执行备份
```bash
cd /root/minecraft-server
./auto-backup-and-push.sh
```

### 2. 查看备份日志
```bash
cat /root/minecraft-server/backup-push.log
# 或实时查看
tail -f /root/minecraft-server/backup-push.log
```

### 3. 查看本地备份文件
```bash
ls -lh /root/minecraft-backups/
```

### 4. 修改备份频率
编辑定时任务：
```bash
crontab -e
```

常用的 cron 时间表达式：
- `0 3 * * *` - 每天凌晨 3:00
- `0 */6 * * *` - 每 6 小时
- `0 */12 * * *` - 每 12 小时
- `0 2 * * 0` - 每周日凌晨 2:00
- `0 0 1 * *` - 每月 1 号凌晨 0:00

### 5. 恢复备份

#### 从本地备份恢复：
```bash
# 停止服务器
screen -S minecraft -X quit

# 恢复备份（替换 YYYYMMDD-HHMMSS 为实际时间）
cd /root
tar -xzf /root/minecraft-backups/minecraft-backup-YYYYMMDD-HHMMSS.tar.gz -C /root/minecraft-server/

# 重启服务器
cd /root/minecraft-server
./start.sh
```

#### 从 GitHub 恢复：
```bash
# 停止服务器
screen -S minecraft -X quit

# 克隆仓库到新目录
cd /root
mv minecraft-server minecraft-server.old
git clone git@github.com:tytsxai/minecraft-server.git

# 重启服务器
cd /root/minecraft-server
./start.sh
```

## ⚙️ 配置说明

### 备份脚本配置 (auto-backup-and-push.sh)
可以在脚本开头修改以下参数：

```bash
SERVER_DIR="/root/minecraft-server"    # 服务器目录
BACKUP_DIR="/root/minecraft-backups"   # 备份目录
MAX_BACKUPS=7                          # 保留最近N个本地备份
MAX_LOG_LINES=1000                     # 日志文件最大行数
```

### 备份脚本工作流程
1. 检测服务器是否运行
2. 如果运行，发送 `save-off` 和 `save-all` 命令
3. 创建压缩备份文件
4. 恢复自动保存
5. 清理旧备份（保留最近 7 个）
6. 提交更改到 git
7. 推送到 GitHub
8. 生成备份报告

## 📊 备份统计

### 查看备份数量
```bash
ls -1 /root/minecraft-backups/*.tar.gz | wc -l
```

### 查看备份目录大小
```bash
du -sh /root/minecraft-backups/
```

### 查看最新备份
```bash
ls -lt /root/minecraft-backups/ | head -n 5
```

## 🔍 故障排查

### 问题1: GitHub 推送失败
**可能原因**: SSH 密钥未配置或网络问题

**解决方案**:
```bash
# 测试 SSH 连接
ssh -T git@github.com

# 如果失败，检查 SSH 密钥
cat ~/.ssh/id_ed25519.pub
# 确保该公钥已添加到 GitHub 账户
```

### 问题2: 备份失败，提示权限错误
**解决方案**:
```bash
# 确保脚本有执行权限
chmod +x /root/minecraft-server/auto-backup-and-push.sh

# 确保备份目录可写
chmod 755 /root/minecraft-backups/
```

### 问题3: 定时任务不执行
**解决方案**:
```bash
# 检查 cron 服务状态
service cron status

# 启动 cron 服务
service cron start

# 查看 cron 日志
grep CRON /var/log/syslog
```

### 问题4: 磁盘空间不足
**解决方案**:
```bash
# 查看磁盘使用情况
df -h

# 减少保留的备份数量
# 编辑脚本，修改 MAX_BACKUPS=7 为更小的值

# 手动删除旧备份
rm /root/minecraft-backups/minecraft-backup-旧日期*.tar.gz
```

## 🌐 GitHub 仓库管理

### 仓库地址
https://github.com/tytsxai/minecraft-server

### 查看远程仓库状态
```bash
cd /root/minecraft-server
git remote -v
git status
```

### 手动推送到 GitHub
```bash
cd /root/minecraft-server
git add .
git commit -m "手动备份"
git push origin main
```

### 从 GitHub 拉取最新版本
```bash
cd /root/minecraft-server
git pull origin main
```

## 💡 最佳实践

1. **定期检查日志**: 每周查看一次 `backup-push.log`，确保备份正常
2. **测试恢复**: 每月测试一次备份恢复流程
3. **监控磁盘空间**: 确保磁盘使用率不超过 90%
4. **保持更新**: 定期检查 GitHub 仓库，确保云端备份最新
5. **重要更新前备份**: 在更新插件或服务器版本前手动执行一次备份

## 📈 性能影响

- **备份时间**: 约 15-20 秒（取决于服务器大小和网络速度）
- **磁盘占用**: 每个备份约 300-400 MB（压缩后）
- **网络流量**: GitHub 推送仅上传修改的文件，通常很小
- **服务器性能**: 备份期间可能有短暂卡顿（1-2秒）

## 🔐 安全建议

1. **SSH 密钥安全**: 不要分享 `~/.ssh/id_ed25519` 私钥
2. **GitHub 仓库设置**: 如有敏感信息，建议设为私有仓库
3. **定期更换密钥**: 建议每 6-12 个月更换一次 SSH 密钥
4. **备份加密**: 对于极其敏感的数据，考虑加密备份文件

## 🆘 紧急恢复指南

如果服务器完全崩溃需要紧急恢复：

```bash
# 1. 从 GitHub 克隆最新版本
cd /root
git clone git@github.com:tytsxai/minecraft-server.git

# 2. 或者从本地备份恢复
mkdir minecraft-server-new
tar -xzf /root/minecraft-backups/minecraft-backup-最新.tar.gz -C minecraft-server-new/

# 3. 启动服务器
cd minecraft-server
./start.sh
```

## 📞 支持

如有问题，请：
1. 查看 `backup-push.log` 日志文件
2. 检查本文档的故障排查部分
3. 查看 GitHub Issues: https://github.com/tytsxai/minecraft-server/issues

---

**最后更新**: 2025-10-06  
**版本**: 1.0

