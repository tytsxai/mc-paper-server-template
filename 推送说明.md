# 🚀 Minecraft 服务器 GitHub 推送指南

## 📋 脚本说明

本脚本 `force-push-to-github.sh` 用于将服务器的**所有内容**安全地推送到 GitHub 仓库。

### ✨ 核心特性

1. ✅ **完全以本地为主** - 不会执行任何 pull/fetch 操作
2. ✅ **强制推送** - 使用 `--force` 确保远程仓库完全同步本地
3. ✅ **自动备份** - 推送前自动创建本地标签，可随时恢复
4. ✅ **安全确认** - 推送前需要手动确认，防止误操作
5. ✅ **详细日志** - 显示所有操作步骤和结果

---

## 🎯 使用方法

### 方法一：直接运行脚本

```bash
cd /root/minecraft-server
./force-push-to-github.sh
```

### 方法二：使用 bash 运行

```bash
cd /root/minecraft-server
bash force-push-to-github.sh
```

---

## 📝 脚本执行流程

脚本会按以下步骤执行：

1. **检查 Git 仓库状态** - 确认当前目录是 Git 仓库
2. **显示远程仓库信息** - 显示 GitHub 仓库地址
3. **显示当前分支** - 显示将要推送的分支名称
4. **创建本地备份标签** - 自动创建时间戳标签（如：backup-before-push-20251005-001234）
5. **添加所有文件** - 执行 `git add -A` 添加所有更改
6. **显示待提交内容** - 预览将要提交的文件列表
7. **提交更改** - 使用时间戳作为提交信息
8. **强制推送** - 使用 `git push --force` 推送到远程仓库

---

## ⚠️ 重要注意事项

### 🔴 强制推送的影响

- **远程仓库会被完全覆盖** - 远程的所有内容将被本地内容替换
- **无法撤销** - 推送后远程的历史记录将被覆盖
- **团队协作** - 如果有其他人在使用这个仓库，他们的更改会丢失

### 🟡 推送前确认

脚本会在推送前暂停，显示：
```
警告：即将执行强制推送，远程仓库将被本地内容完全覆盖！
推送目标：origin/main
按 Enter 继续，或按 Ctrl+C 取消...
```

- 按 **Enter** 键继续推送
- 按 **Ctrl+C** 取消操作

---

## 🔄 如何恢复

如果推送后发现问题，可以使用自动创建的备份标签恢复：

```bash
# 查看所有备份标签
git tag | grep backup-before-push

# 恢复到指定标签
git reset --hard backup-before-push-20251005-001234

# 如果需要，再次强制推送恢复后的状态
git push --force origin main
```

---

## 📊 推送的内容

根据 `.gitignore` 配置，以下内容**不会**被推送：

### ❌ 排除的内容
- `*.log` - 所有日志文件
- `logs/` - 日志目录
- `world/` - 主世界数据
- `world_nether/` - 下界数据
- `world_the_end/` - 末地数据
- `cache/` - 缓存目录
- `*.tmp` - 临时文件

### ✅ 包含的内容
- 服务器核心文件（paper-*.jar）
- 所有插件文件（plugins/）
- 配置文件（*.yml, *.properties）
- 玩家数据（plugins/*/PlayerData/）
- 权限数据（LuckPerms 数据库）
- 其他重要配置

---

## 🛡️ 安全特性

### 1. 本地备份标签
每次推送前自动创建标签，格式：
```
backup-before-push-YYYYMMDD-HHMMSS
```

### 2. 不会拉取远程内容
脚本**绝不执行**以下命令：
- ❌ `git pull`
- ❌ `git fetch`
- ❌ `git merge`
- ❌ `git rebase`

### 3. 强制推送保护
使用 `--force` 而不是 `--force-with-lease`，确保完全以本地为主。

---

## 💡 使用场景

### 适合使用的情况
✅ 需要完整备份服务器到 GitHub
✅ 本地是唯一的数据源
✅ 远程仓库需要与本地完全同步
✅ 定期备份服务器配置和插件

### 不适合使用的情况
❌ 多人协作开发
❌ 需要保留远程的更改
❌ 不确定本地数据是否正确

---

## 🔍 故障排除

### 问题 1：推送失败 - 权限错误
```
Permission denied (publickey)
```

**解决方案**：
```bash
# 检查 SSH 密钥
ssh -T git@github.com

# 如果失败，需要配置 SSH 密钥
# 参考：https://docs.github.com/cn/authentication/connecting-to-github-with-ssh
```

### 问题 2：推送失败 - 仓库不存在
```
remote: Repository not found
```

**解决方案**：
```bash
# 检查远程仓库地址
git remote -v

# 更新远程仓库地址
git remote set-url origin git@github.com:用户名/仓库名.git
```

### 问题 3：文件太大无法推送
```
remote: error: File ... is ... MB; this exceeds GitHub's file size limit
```

**解决方案**：
```bash
# 将大文件添加到 .gitignore
echo "大文件路径" >> .gitignore
git add .gitignore
git commit -m "排除大文件"
```

---

## 📞 技术支持

- GitHub 仓库：https://github.com/tytsxai/mc-java-fuwuqi
- 当前分支：main
- 远程名称：origin

---

## 📅 更新日志

- **2025-10-05**: 创建初始版本
  - 实现强制推送功能
  - 添加自动备份标签
  - 添加安全确认机制

---

## ⚡ 快速参考

```bash
# 推送服务器到 GitHub
./force-push-to-github.sh

# 查看备份标签
git tag | grep backup

# 恢复到推送前状态
git reset --hard backup-before-push-YYYYMMDD-HHMMSS

# 查看远程仓库
git remote -v

# 查看当前分支
git branch

# 查看提交历史
git log --oneline -10
```

---

**🎮 祝您的 Minecraft 服务器运行愉快！**
